@page "/invoices/{id:int}"
@rendermode InteractiveServer
@using Microsoft.AspNetCore.Authorization
@attribute [Authorize]

<h3>InvoiceDetails for invoice @id</h3>

@code {
    [Parameter]
    public int id { get; set; }
}
